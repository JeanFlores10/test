pipeline {
    agent any
    stages {
        stage("build"){
            steps{
                echo 'building the application'
                echo "SECRET_URL = ${env.GIT_URL}"
                echo "BUILD_NUMBER = ${env.BUILD_NUMBER}"
            }
        }

        stage("test"){
            steps{
                echo 'testing the application'
            }
        }

        stage("deploy"){
            steps{
                echo 'deplying the application'
            }
        }

    }
}